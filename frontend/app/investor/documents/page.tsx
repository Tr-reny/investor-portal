'use client';
import React,{useEffect,useState} from 'react'; import { api } from '@/lib/api'; import Link from 'next/link';
export default function InvestorDocuments(){ const [docs,setDocs]=useState<any[]>([]); useEffect(()=>{ api.get('/documents').then(setDocs); },[]);
return(<main className="max-w-7xl mx-auto p-4 grid grid-cols-12 gap-4"><aside className="col-span-12 lg:col-span-3 space-y-4"><div className="card"><div className="card-header">Navigation</div><div className="card-content space-y-2"><Link className="sidenav-btn" href="/investor/dashboard">Overview</Link><Link className="sidenav-btn" href="/investor/documents">Documents</Link></div></div></aside><section className="col-span-12 lg:col-span-9"><div className="card"><div className="card-header">Documents & Disclosures</div><div className="card-content"><ul className="list-disc pl-5 space-y-1 text-neutral-700">{docs.map(d=>(<li key={d.id}><span className="font-medium">{d.title}</span>{' '}{d.period && <em className="text-neutral-500">({d.period})</em>} – <code>{d.fileUrl}</code></li>))}</ul></div></div></section></main>); }
