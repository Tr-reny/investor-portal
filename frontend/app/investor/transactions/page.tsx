'use client';
import React,{useEffect,useState} from 'react'; import { api } from '@/lib/api'; import Link from 'next/link';
const fmt=new Intl.NumberFormat(undefined,{style:'currency',currency:'USD'});
export default function InvestorTransactions(){ const [rows,setRows]=useState<any[]>([]); useEffect(()=>{ api.get('/transactions').then(setRows); },[]);
return(<main className="max-w-7xl mx-auto p-4 grid grid-cols-12 gap-4"><aside className="col-span-12 lg:col-span-3 space-y-4"><div className="card"><div className="card-header">Navigation</div><div className="card-content space-y-2"><Link className="sidenav-btn" href="/investor/dashboard">Overview</Link><Link className="sidenav-btn" href="/investor/transactions">Transactions</Link><Link className="sidenav-btn" href="/investor/documents">Documents</Link></div></div></aside><section className="col-span-12 lg:col-span-9"><div className="card"><div className="card-header">Recent Transactions</div><div className="card-content overflow-x-auto"><table className="table"><thead><tr><th>Date</th><th>Type</th><th>Investor</th><th className="text-right">Amount</th><th>Status</th></tr></thead><tbody>{rows.map(r=>(<tr key={r.id}><td>{r.date}</td><td>{r.type}</td><td>{r.investor}</td><td className="text-right">{fmt.format(r.amount)}</td><td>{r.status}</td></tr>))}</tbody></table></div></div></section></main>); }
